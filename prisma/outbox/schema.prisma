model Outbox {
    id         String   @id @default(cuid())
    exchange   String
    routingKey String?
    payload    String
    aggregate  Json?
    isSent     Boolean  @default(false)
    createdAt  DateTime @default(now())
}
